import React from 'react';
import gsap from "gsap";

import Button from './Button';

function Menu() {

  return (
    <div className="menu">
        <Button />
        <Button />
        <Button />
        <Button />
        <Button />
    </div>
  );
}

export default Menu;